# python_code
Adding two numbers in python

# write a python program of two number adding.

# take user input the value of a and b.
a = int(input("enter first number: "))
b = int(input("enter second number:"))

add = a+b       # add a and b value.

print("value of addition is : ",add)    # print sum of a and b 

print(type(a))                # refers data types of a
print(id(add))                # refers id of a
print(type(add))            # refers data types of add
